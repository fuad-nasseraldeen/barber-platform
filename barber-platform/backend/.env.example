# ===========================
# DATABASE (Supabase PostgreSQL)
# ===========================
DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@HOST:5432/postgres"

# ===========================
# REDIS (Upstash)
# ===========================
# Set to false to run without Redis (development mode). No connections, queues, or workers.
# ENABLE_REDIS=false
# Set to false to disable Bull queue workers (reduces Redis requests; use when hitting Upstash limits)
# ENABLE_QUEUE_WORKERS=false
REDIS_HOST=xxx.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=xxx
REDIS_TLS=true
REDIS_URL="rediss://default:PASSWORD@HOST:6379"

# ===========================
# Supabase Storage
# ===========================
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=xxx
SUPABASE_BUCKET=tenant-assets
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx

# ===========================
# Google OAuth
# ===========================
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_MAPS_KEY=xxx

# ===========================
# SMS (sms.to)
# ===========================
SMS_TO_API_KEY=xxx
# Sender ID: alphanumeric + spaces only (e.g. Toren, MyBarber). Default: Toren
# SMS_SENDER_ID=SMS OTP

# ===========================
# JWT
# ===========================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Access token: short-lived (15m). Refresh extends session.
JWT_EXPIRES_IN=15m
# Refresh token: sliding expiration. Each refresh gives new token with this duration.
# 90d = stay logged in as long as app used at least once per 90 days.
JWT_REFRESH_EXPIRES_IN=90d

# ===========================
# Stripe
# ===========================
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# ===========================
# App
# ===========================
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1
APP_URL=http://localhost:3000
# Comma-separated origins for CORS (default: localhost:3001, localhost:3000)
# CORS_ORIGIN=http://localhost:3001,http://127.0.0.1:3001
