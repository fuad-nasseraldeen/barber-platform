[phases.setup]
# Keep Node version aligned with backend requirements (Node 18+)
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"
