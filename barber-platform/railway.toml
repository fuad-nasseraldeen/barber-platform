[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run --prefix backend start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
